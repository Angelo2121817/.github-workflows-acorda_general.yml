name: Manter Site Vivo
on:
  schedule:
    - cron: "0 */5 * * *" # Roda a cada 5 horas
  workflow_dispatch: # Botão pra você testar manualmente se quiser

jobs:
  ping_site:
    runs-on: ubuntu-latest
    steps:
      - name: Acorda Metal Quimica
        run: curl -I https://sistema-labortec-fptkmmnvsfzalhpt33g4ev.streamlit.app/
